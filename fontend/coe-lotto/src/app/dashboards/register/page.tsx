import wait from "@/app/@lib/wait";

export default async function Register() {
  await wait(5000);
  return (
    <div>
      <h1>Check</h1>
      <p>Check will be listed here.</p>
    </div>
  ); 
}